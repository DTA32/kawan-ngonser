# Base URL of the Kawan Ngonser backend (TR-3). Baked at BUILD time — the
# generated static output cannot read env vars at runtime.
# API surface: GET /concerts, GET /concerts/:id, GET /concerts/:id/version, GET /config
NUXT_PUBLIC_API_BASE=http://localhost:3001

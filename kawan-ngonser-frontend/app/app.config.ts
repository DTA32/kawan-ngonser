export default defineAppConfig({
  ui: {
    colors: {
      primary: 'violet',
      neutral: 'slate',
    },
    button: {
      slots: {
        base: 'rounded-full font-semibold justify-center',
      },
    },
  },
})
